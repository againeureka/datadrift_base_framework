# ddoc v2.0.4 릴리스 노트

## 릴리스 일자
2025-12-23

## 🎯 Snapshot 안정화 및 CLI 개편

ddoc v2.0.4는 스냅샷 생성/복원(체크아웃) 플로우의 안정성을 크게 개선하고, `snapshot` 명령 구조를 “핵심은 서브커맨드, 부가 기능은 옵션” 형태로 정리한 릴리스입니다.

## 🐛 Bug Fixes

### 1. 초기 워크스페이스에서 스냅샷 생성 실패 (`data.dvc not found`)
- **문제**: 초기화 직후(데이터 추가 전) `ddoc snapshot create ...` 시 `data.dvc`가 없어 실패
- **원인**: `_auto_commit_workflow`가 `data.dvc`가 없고 `data/`가 비어있을 때 `dvc add`를 수행하지 않음
- **해결**: `data.dvc`가 없으면 `data/`가 비어있더라도 초기 `dvc add data/`를 수행하도록 개선

### 2. 체크아웃 실패: `.ddoc` 변경 때문에 Git checkout이 막히는 문제
- **문제**: 스냅샷 체크아웃 시 `You have uncommitted changes` 또는 `.ddoc/* would be overwritten` 에러 발생
- **원인**: `.ddoc` 메타데이터가 체크아웃 과정에서 변경되며 Git이 이를 “작업 트리 변경”으로 판단
- **해결**:
  - 체크아웃 전 변경 감지에서 `.ddoc` 변경을 무시하도록 정리
  - **중요**: 스냅샷 메타데이터(`.ddoc/`)는 Git에서 제외하여 체크아웃으로 스냅샷 히스토리가 사라지지 않도록 개선 (Git의 `.git/`, DVC의 `.dvc/`와 동일한 패턴)

### 3. 체크아웃 후 데이터 폴더에 빈 디렉터리 구조가 남는 문제
- **문제**: `v02 -> v01` 체크아웃 후 실제 파일은 삭제되지만 `train/valid/test` 같은 빈 폴더가 남음
- **원인**:
  - DVC는 “파일”만 관리하므로 빈 디렉터리는 남을 수 있음
  - macOS 환경에서는 `.DS_Store`가 남아 디렉터리가 “완전히 비어있지 않아서” 정리되지 않는 경우가 발생
- **해결**:
  - 체크아웃 후 `data/` 하위에서 빈 디렉터리를 정리하는 후처리 추가
  - `.DS_Store`, `._*` 등 정크 파일을 제거한 뒤 빈 디렉터리를 제거하도록 개선

## ✨ Improvements

### 1. Snapshot CLI 하이브리드 구조로 개편
핵심 기능은 서브커맨드로 분리하고, 덜 쓰는 기능은 옵션으로 유지했습니다.

- **핵심(서브커맨드)**:
  - `ddoc snapshot create -m "message" [-a alias] [--no-auto-commit]`
  - `ddoc snapshot list [--oneline] [-n limit]`
  - `ddoc snapshot inspect <version|alias>`
  - `ddoc snapshot checkout <version|alias> [-f|--force]`
- **부가(옵션)**:
  - `ddoc snapshot --diff v01 v02`
  - `ddoc snapshot --graph`
  - `ddoc snapshot --delete v01 [--force]`
  - `ddoc snapshot --set-alias v01 baseline`, `--unalias baseline`, `--show-aliases`
  - `ddoc snapshot --verify v01`, `--verify-all`
  - `ddoc snapshot --prune`

또한 **옵션/서브커맨드가 아무 것도 없을 때는 경고와 함께 에러 종료**하도록 바꿨습니다(실수 방지).

## 📚 Updated Documentation

- `docs/releases/v2.0.4.md` (본 문서)
- `docs/changelog.md`에 2.0.4 항목 추가

---

**전체 변경 이력**: v2.0.3과 v2.0.4 사이의 git log 참조


